# AI Targetologist Pro

Premium mobile-first AI marketing assistant built with React + Vite + TypeScript.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The AI generation is currently mocked so the interface works without API keys.